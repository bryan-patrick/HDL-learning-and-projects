## reg

reg is short for "register", which is actually a little confusing in Verilog since it doesn't necessarily mean memory register.

It's an old convention, it will later be replaced with "logic" when we move on to System Verilog.
